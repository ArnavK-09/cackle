export default function (title) {
    let data = {
        title: title + ' | Cackle',
        meta: [
          { name: 'description', content: 'My amazing site.' }
        ],
      };

    return data;
}